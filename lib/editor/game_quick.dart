import 'package:flutter/material.dart';

class MyGameQuick extends StatelessWidget {
  const MyGameQuick({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
